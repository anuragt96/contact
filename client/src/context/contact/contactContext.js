import { createContext } from 'react';

const contactContext = createContext();

export default contactContext;



//this file is only created for initialization of context which is used for state mnmgt